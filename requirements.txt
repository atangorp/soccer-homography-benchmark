# requirements.txt
# ==================
# Dependency manifest untuk soccer-homography-benchmark.
# Dibagi per grup karena tiap keluarga model punya environment yang berbeda.
#
# URUTAN INSTALL DI COLAB:
#   1. Group A (selalu) — jangan skip
#   2. Group B (YOLO11) — untuk notebook 02a
#   3. Group C (MMPose) — untuk notebook 02b + 02c
#   4. Group D (DETR)   — untuk notebook 02d
#
# ⚠️  Jangan install semua sekaligus — konflik dependency antar group
#     sangat mungkin terjadi. Install per notebook sesuai urutan di atas.

# ── Group A: Core (semua notebook) ───────────────────────────────────────────
numpy>=1.24.0
opencv-python-headless>=4.8.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0
pyyaml>=6.0
tqdm>=4.65.0
pycocotools>=2.0.7
Pillow>=9.4.0

# ── Group B: YOLO11 (notebook 02a) ───────────────────────────────────────────
# Install: pip install ultralytics
ultralytics>=8.3.0

# ── Group C: MMPose stack (notebook 02b + 02c) ───────────────────────────────
# Install via mim (BUKAN pip langsung):
#   pip install -U openmim
#   mim install mmengine
#   mim install 'mmcv>=2.0.0'
#   mim install mmdet
#   mim install mmpose
#
# Versi yang diuji kompatibel:
#   mmengine==0.10.4
#   mmcv==2.1.0
#   mmdet==3.3.0
#   mmpose==1.3.1

# ── Group D: Deformable DETR (notebook 02d) ──────────────────────────────────
# Install secara manual dari source — BUKAN dari PyPI:
#   git clone https://github.com/fundamentalvision/Deformable-DETR.git
#   cd Deformable-DETR/models/ops
#   python setup.py build install
#
# Dependencies DETR:
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.0
einops>=0.7.0

# ── Evaluation & paper (notebook 03–05) ──────────────────────────────────────
# (semua sudah tercakup di Group A)
